/**
 * _strlen(char *s) - strlen but worse
 * Description: Get the length of a string
 * Return: length of string
 */
int _strlen(char *s)
{
	int idx;

	idx = 0;
	for(; *(s + idx) != '\0'; idx++)

	return (idx);


}
