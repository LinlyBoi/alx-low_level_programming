#include "main.h"
/**
 * _isalpha - letter? YES
 * Description: A, a :), 0 :(
 * @c: the character you wanna check
 * Return: 1 if letter (any case), 0 otherwise
 */
int _isalpha(int c)
{
		return (1);
}
